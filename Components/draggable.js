import React from "react";
import { View, Text } from "react-native"

export function Draggable() {
    return(
        <View style={{height: "100%", backgroundColor: "blue", }}>
            <Text>Some more stuff...</Text>
        </View>
    )
}